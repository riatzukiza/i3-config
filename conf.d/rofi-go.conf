# Rofi -> emacsclient bridge launcher mode.
# DISABLED: rofi list was too noisy; using Emacs-native Consult/Vertico popup
# instead (see consult-launcher.conf).  Kept for reference.
#
# set $mode_go Go: [f]iles [b]uffers [p]rojects [t]erminals [w]orkspaces [a]gents [Esc]
#
# mode "$mode_go" {
#     bindsym Escape mode "default"
#     bindsym Return mode "default"
#
#     bindsym f exec --no-startup-id ~/.local/bin/wm-go files; mode "default"
#     bindsym b exec --no-startup-id ~/.local/bin/wm-go buffers; mode "default"
#     bindsym p exec --no-startup-id ~/.local/bin/wm-go projects; mode "default"
#     bindsym t exec --no-startup-id ~/.local/bin/wm-go terminals; mode "default"
#     bindsym w exec --no-startup-id ~/.local/bin/wm-go workspaces; mode "default"
#     bindsym a exec --no-startup-id ~/.local/bin/wm-go agents; mode "default"
# }
#
# bindsym $mod+g mode "$mode_go"
# bindsym $mod+Shift+Return exec --no-startup-id emacsclient -n -e '(my/new-vterm-dispatch)'
